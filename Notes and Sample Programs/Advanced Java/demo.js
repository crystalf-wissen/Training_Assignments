function abc() {
    print("Function abc called!");
}

function add(a, b) {
    print("Adding numbers: " + a + " + " + b);
    return a + b;
}

function greet(name, field) {
    return "Hello " + name + " from " + field + "!";
}
